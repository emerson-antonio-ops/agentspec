"""AgentSpec MCP server package.

Exposes KB search, agent routing, SDD status and judge tooling through the
Model Context Protocol so non-Claude-Code clients can consume the same
context AgentSpec ships natively.
"""

__version__ = "0.1.0"
